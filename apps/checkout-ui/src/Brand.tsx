/**
 * The Mayarin mark (#151): the same electric square the favicon carries, at
 * wordmark size. The merchant stays the primary identity on every page — this
 * is the quiet "powered by" line, not a masthead.
 */
export function Brand() {
  return <div className="brand">Mayarin</div>;
}
