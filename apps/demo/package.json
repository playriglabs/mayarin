{
  "name": "@mayarin/demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --bun run vite",
    "dev:demo": "bun --bun run vite",
    "build": "bun --bun run vite build && bun run verify-bundle.ts && bun --bun x wrangler deploy --config wrangler.worker.jsonc --dry-run --outdir .wrangler/demo-worker && cp .wrangler/demo-worker/worker.js dist/_worker.js",
    "deploy": "bun run build && CI=1 bunx wrangler pages deploy dist --project-name mayarin-demo --branch main --commit-dirty=true --skip-caching",
    "seed": "bun run seed.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mayarin/notifications": "workspace:*",
    "@mayarin/sdk": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "tailwindcss": "^4.1.18",
    "vite": "^7.1.12"
  }
}
