{
  "name": "@mayarin/landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "bun run typecheck && bun run build && wrangler d1 migrations apply mayarin-landing-early-access --remote && wrangler pages deploy --branch main",
    "early-access:list": "wrangler d1 execute mayarin-landing-early-access --remote --command \"select email, datetime(created_at, 'unixepoch') as submitted_at from early_access_signups order by created_at desc\"",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "cobe": "^2.0.1",
    "lenis": "^1.3.25",
    "motion": "^13.0.0",
    "preact": "^10.27.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "@preact/preset-vite": "^2.10.2",
    "@tailwindcss/vite": "^4.1.18",
    "shiki": "^4.4.1",
    "tailwindcss": "^4.1.18",
    "vite": "^7.1.12",
    "wrangler": "^4.120.1"
  }
}
