{
  "name": "@mayarin/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Typed PaymentRouter ABI + Order/PaymentCompleted domain types",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:abi": "bun run scripts/generate-abi.ts",
    "build:order-vectors": "bun run scripts/generate-order-vectors.ts"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@mayarin/quote": "workspace:*"
  }
}
