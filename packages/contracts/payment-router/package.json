{
  "name": "@mayarin/contracts-payment-router",
  "private": true,
  "version": "0.0.0",
  "description": "PaymentRouter.sol — on-chain execution layer",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "gas": "forge snapshot",
    "install-deps": "forge soldeer install"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.3.0"
  }
}
